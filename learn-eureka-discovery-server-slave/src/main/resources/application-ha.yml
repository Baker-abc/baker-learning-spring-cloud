
server:
  port: 5003

spring:
  application:
    name: eureka-discovery-server-slave

eureka:
  instance:
    hostname: discovery-server-slave
  client:
    service-url:
      # EurekaServer的地址，现在是自己的地址，如果是集群，需要加上其它Server的地址。
      defaultZone: http://discovery-server:5001/eureka/


